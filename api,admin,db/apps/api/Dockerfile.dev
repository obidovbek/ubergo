FROM node:22-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies (including dev dependencies)
RUN npm install

# Copy source code
COPY . .

# Expose port
EXPOSE 4000

# Set environment
ENV NODE_ENV=development

# Use polling-based file watching for Docker compatibility
CMD ["npm", "run", "dev:poll"]

