---
description: Review recent changes — bugs, security, rule violations
---

Review the latest changes (use git diff if available, otherwise the most
recently edited files).

Look for: bugs and logic errors; security problems (secrets in code, unsafe
input handling, injections); violations of the Working rules in CLAUDE.md;
missing tests.

Report as a short list, worst problem first, with file and line.
Do NOT fix anything without my approval.
